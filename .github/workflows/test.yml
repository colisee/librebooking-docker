name: Test 1

on:
  workflow_dispatch:

jobs:
  get-docker-latest-tag:
    runs-on: ubuntu-latest
    steps:
      -
        name: git clone librebooking/docker repository
        run: |
          git clone https://github.com/librebooking/docker
          cd docker
          echo $(git describe --tags)
          imgTag=$(git describe --tags | sed -e "s/^[vV]//")
          echo "imgTag=$imgTag" >> $GITHUB_OUTPUT